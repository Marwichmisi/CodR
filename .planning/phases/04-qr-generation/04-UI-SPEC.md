---
phase: 4
slug: qr-generation
status: approved
shadcn_initialized: false
preset: not applicable (Flutter/Material 3)
created: 2026-06-28
reviewed_at: 2026-06-28
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for QR Generation screen. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Flutter Material 3) |
| Preset | not applicable |
| Component library | Material 3 (Flutter built-in) |
| Icon library | Material Icons (uses-material-design: true) |
| Font | Google Fonts Inter (`GoogleFonts.interTextTheme()`) |

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, counter text |
| sm | 8px | Compact element spacing, button internal padding |
| md | 16px | Default element spacing, section padding |
| lg | 24px | Between QR preview and action buttons |
| xl | 32px | Major layout gaps (placeholder icon to title) |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions: none

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 (regular) | 1.5 |
| Label | 14px | 400 (regular) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.1 |

Note: Typography derived from existing `AppTheme` which uses `GoogleFonts.interTextTheme()`. All text must be in French per project convention.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` (white) | Background surfaces, main screen area |
| Secondary (30%) | `#F0F4F8` (light gray) | TextField background, QR preview card background |
| Accent (10%) | `#87CEEB` (sky blue) | Action buttons (Sauvegarder, Partager, Copier), URL badge, character counter when at limit |
| Destructive | `#D32F2F` (red) | Error SnackBars only |

Accent reserved for:
- Action buttons (Sauvegarder, Partager, Copier) — background fill
- URL detected badge — background and border
- Character counter at 250/250 — text color change to indicate limit reached

---

## Screen Layout Contract

### Overall Structure (vertical column)

```
┌─────────────────────────────────┐
│ AppBar: "Générateur"            │
├─────────────────────────────────┤
│                                 │
│  ┌───────────────────────────┐  │
│  │ TextField (multiline)     │  │  ← Top: text input
│  │ Counter: n/250            │  │
│  └───────────────────────────┘  │
│                                 │
│  ┌───────────────────────────┐  │
│  │ URL Badge (conditional)   │  │  ← Below input, shown if URL detected
│  └───────────────────────────┘  │
│                                 │
│      ┌─────────────────┐       │
│      │   QR Preview     │       │  ← Center: QrImage 300x300
│      │   (or placeholder│       │
│      │    when empty)   │       │
│      └─────────────────┘       │
│                                 │
│  ┌───────────────────────────┐  │
│  │ [Sauvegarder] [Partager]  │  │  ← Bottom: action buttons row
│  │        [Copier]           │  │
│  └───────────────────────────┘  │
│                                 │
└─────────────────────────────────┘
```

### Responsive Behavior
- Wrap body in `LayoutBuilder` + `ConstrainedBox(maxWidth: 480)` (existing pattern from `GeneratorScreen`)
- On screens wider than 600dp, constrain content to 480dp centered
- QR preview scales proportionally within constrained width

### Element Specifications

#### TextField
- `maxLines: 4`, `minLines: 2`
- `maxLength: 250` (built-in counter)
- Counter format: `n/250`
- Counter text color: `colorScheme.primary` normally, changes to `#D32F2F` (destructive) at 250/250
- `decoration: InputDecoration(hintText: "Entrez du texte ou une URL...")`

#### URL Badge
- Shown only when URL is detected (text contains `.` and no spaces, or starts with `http://`, `https://`, `www.`)
- Style: `Container` with `RoundedRectangleBorder(borderRadius: 8px)`
- Background: `colorScheme.primary` (sky blue) at 10% opacity
- Border: 1px solid `colorScheme.primary`
- Text: "URL détectée" in `Label` size (14px), weight 400
- Icon: `Icons.link` (16px) before text
- Position: directly below TextField, with `SizedBox(height: 8)` spacing

#### QR Preview / Placeholder
- **When text is empty**: Center-aligned column with `Icons.qr_code` (64px, `colorScheme.primary`) and text "Saisissez du texte pour générer un QR code" (`Body` size, `colorScheme.onSurfaceVariant`)
- **When text is present**: `QrImageView` widget from `qr_flutter`, data = decoded text (with `https://` prefix if URL detected), size = 300px (per D-01: fixed 300x300px output), eyeStyle/fill/color = black/white (default)
- **Debounce**: 300ms — QR updates 300ms after user stops typing

#### Action Buttons
- Row of 3 buttons below QR preview with `SizedBox(height: 24)` spacing above
- Each button: `FilledButton` (primary) or `OutlinedButton` (secondary)
- Layout: Row with `MainAxisAlignment.spaceEvenly`
- Button 1: "Sauvegarder" — `Icons.save_alt` — triggers gallery save
- Button 2: "Partager" — `Icons.share` — triggers native share
- Button 3: "Copier" — `Icons.content_copy` — copies text to clipboard
- Button width: flexible, equal distribution
- Button height: 48px (Material 3 default touch target)
- Button icon size: 20px
- Button label: 14px, weight 500

#### SnackBar Feedback
- Position: `SnackBarBehavior.floating`
- Duration: 2 seconds
- Messages:
  - Save success: "QR sauvegardé !" (green/background color)
  - Save error: "Erreur lors de la sauvegarde" (red/destructive)
  - Share success: no SnackBar (share sheet is self-evident)
  - Copy success: "Copié !" (green/background color)
  - Permission denied: "Permission galerie refusée. Activez-la dans les réglages." (red/destructive)

---

## Copywriting Contract

| Element | Copy (French) |
|---------|---------------|
| Primary CTA | "Sauvegarder" (save QR to gallery) |
| Secondary CTA 1 | "Partager" (share QR image) |
| Secondary CTA 2 | "Copier" (copy text to clipboard) |
| Empty state heading | "Saisissez du texte pour générer un QR code" |
| Empty state body | (none — heading is sufficient) |
| Error state | "Erreur lors de la sauvegarde. Réessayez." |
| Error state (permission) | "Permission galerie refusée. Activez-la dans les réglages." |
| URL detected badge | "URL détectée" |
| TextField hint | "Entrez du texte ou une URL..." |
| AppBar title | "Générateur" |
| SnackBar save | "QR sauvegardé !" |
| SnackBar copy | "Copié !" |

Destructive actions: None in this phase. No delete or destructive confirmation needed.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | n/a | not applicable |

No shadcn or third-party registries. Flutter Material 3 only.

---

## Flutter Skills Reference

The executor MUST apply these Flutter skills during implementation:

| Skill | When to Apply |
|-------|---------------|
| `flutter-apply-architecture-best-practices` | Creating `GeneratorViewModel` following MVVM pattern, folder structure (`lib/viewmodels/`, `lib/screens/`) |
| `flutter-add-widget-test` | Writing widget tests for `GeneratorScreen` (empty state, text input, 250 limit, button actions, SnackBar feedback) |
| `flutter-add-widget-preview` | Adding `@Preview` annotations to `GeneratorScreen` and any extracted widgets |
| `flutter-build-responsive-layout` | Using `LayoutBuilder` + `ConstrainedBox(maxWidth: 480)` for responsive layout |
| `flutter-fix-layout-issues` | Resolving any overflow or unbounded height issues in the column layout |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: FLAG (recommendation: add explicit focal point declaration)
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** APPROVED — 2026-06-28
**Recommendation:** Add focal point declaration: "Primary visual anchor: QR Preview (center). Visual hierarchy flows: TextField → QR Preview → Action Buttons."

---

## Upstream Decisions Used

| Source | Count | Key Decisions |
|--------|-------|---------------|
| CONTEXT.md | 9 | D-01 (qr_flutter), D-02 (vertical layout), D-03 (debounce 300ms), D-04 (URL detection), D-05 (permission on click), D-06 (ChangeNotifier pattern), D-07 (ViewModel state), D-08 (ListenableBuilder), D-09 (SnackBar feedback) |
| SPEC.md | 7 | All 7 requirements locked with acceptance criteria |
| REQUIREMENTS.md | 7 | GEN-01 through GEN-07 |
| AppTheme | 1 | Material 3 seed #87CEEB, Inter font |
| Existing code | 3 | GeneratorScreen placeholder, ScannerViewModel pattern, LayoutBuilder pattern |

**Pre-populated from upstream:** All major decisions. No user questions needed — CONTEXT.md and SPEC.md fully answer the design contract.

---

*Phase: 04-qr-generation*
*UI-SPEC created: 2026-06-28*
*Ready for: gsd-ui-checker validation*
