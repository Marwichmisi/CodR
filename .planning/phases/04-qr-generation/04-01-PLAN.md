---
phase: 04-qr-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qr_scanner/pubspec.yaml
  - qr_scanner/lib/viewmodels/generator_viewmodel.dart
  - qr_scanner/lib/services/permission_service.dart
autonomous: true
requirements:
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-05
  - GEN-07
must_haves:
  truths:
    - "User types text and QR preview updates after a 300ms pause (debounce)"
    - "User types a URL (e.g. 'example.com') and QR encodes it with https:// prepended"
    - "User types a URL with a scheme already present (e.g. 'https://flutter.dev') and QR encodes it as-is without double-prepending"
    - "User rapidly types multiple characters within 300ms and only the final value triggers a QR update"
    - "User taps copy button and the raw input text (not the QR-encoded version) is placed on the clipboard"
    - "Save-to-gallery request asks for gallery permission when triggered by user action"
    - "Gallery permission request falls back to Permission.storage on Android when Permission.photos is not available"
    - "No logging, network calls, or telemetry transmits user-generated content"
    - "Save action does not trigger automatically on text input or screen load — only on explicit user tap"
  artifacts:
    - "qr_scanner/lib/viewmodels/generator_viewmodel.dart"
    - "qr_scanner/lib/services/permission_service.dart"
  key_links:
    - "GeneratorViewModel.updateText -> debounce timer -> _qrText notification"
    - "PermissionService.gallery methods -> permission_handler Permission.photos"
---

<objective>
Create the GeneratorViewModel with debounce, URL detection, clipboard copy, and gallery permission support. Extend PermissionService with gallery permission methods.

Purpose: The ViewModel is the core business logic layer — without it, the screen has no state to drive QR rendering or handle user actions. The permission service extension enables the save-to-gallery flow in Plan 04-02.

Output: `generator_viewmodel.dart` (new), `permission_service.dart` (modified), `pubspec.yaml` (3 new dependencies)
</objective>

<execution_context>
@/home/marwane/Documents/CodR/.opencode/gsd-core/workflows/execute-plan.md
@/home/marwane/Documents/CodR/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-qr-generation/04-SPEC.md
@.planning/phases/04-qr-generation/04-RESEARCH.md
@.planning/phases/04-qr-generation/04-CONTEXT.md
@.planning/phases/04-qr-generation/04-UI-SPEC.md
@.planning/phases/04-qr-generation/04-PATTERNS.md
@qr_scanner/lib/viewmodels/scanner_viewmodel.dart
@qr_scanner/lib/services/permission_service.dart
@qr_scanner/pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and extend PermissionService with gallery permissions</name>
  <files>qr_scanner/pubspec.yaml, qr_scanner/lib/services/permission_service.dart</files>

  <read_first>
    - qr_scanner/pubspec.yaml (current dependencies — path_provider and permission_handler already present)
    - qr_scanner/lib/services/permission_service.dart (abstract interface and SystemPermissionService implementation)
    - qr_scanner/test/viewmodels/scanner_viewmodel_test.dart (mock pattern to follow for gallery permission tests)
  </read_first>

  <action>
1. Run `flutter pub add qr_flutter share_plus saver_gallery` in the project root to install the three new dependencies. Verify they appear in pubspec.yaml under `dependencies:`.

2. Open `qr_scanner/lib/services/permission_service.dart`. Add two new abstract methods to the `PermissionService` class:
   - `Future<bool> hasGalleryPermission();`
   - `Future<bool> requestGalleryPermission();`

3. In `SystemPermissionService`, implement both methods:
   - `hasGalleryPermission()`: check `Permission.photos.status.isGranted` (covers iOS) with a fallback to `Permission.storage.status.isGranted` for Android. Return true if either is granted.
   - `requestGalleryPermission()`: call `Permission.photos.request()` first. If not granted, fall back to `Permission.storage.request()`. Return `status.isGranted` from whichever succeeded.

4. Keep the existing camera permission methods unchanged — do not remove or refactor them.

5. Verify the package install succeeded: run `flutter pub deps | grep qr_flutter` and `flutter pub deps | grep saver_gallery` — both must show output.
  </action>

  <acceptance_criteria>
    - `qr_scanner/pubspec.yaml` contains `qr_flutter`, `share_plus`, and `saver_gallery` under `dependencies:`
    - `PermissionService` abstract class declares `hasGalleryPermission()` and `requestGalleryPermission()` methods
    - `SystemPermissionService` implements both methods using `Permission.photos` with `Permission.storage` fallback
    - `flutter pub deps | grep qr_flutter` returns non-empty output
    - `flutter pub deps | grep saver_gallery` returns non-empty output
    - Existing camera permission methods (`hasCameraPermission`, `requestCameraPermission`, `openSettings`) are unchanged
  </acceptance_criteria>

  <verify>
    <automated>cd qr_scanner && flutter pub deps | grep qr_flutter && flutter pub deps | grep saver_gallery && flutter pub deps | grep share_plus</automated>
  </verify>

  <done>
    Three packages installed, PermissionService extended with gallery methods, existing camera methods unchanged, flutter pub get succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GeneratorViewModel with debounce, URL detection, and clipboard copy</name>
  <files>qr_scanner/lib/viewmodels/generator_viewmodel.dart, qr_scanner/test/viewmodels/generator_viewmodel_test.dart</files>

  <behavior>
    - updateText("hello") then pump 300ms: qrText becomes "hello", isUrlDetected is false
    - updateText("example.com") then pump 300ms: qrText becomes "https://example.com", isUrlDetected is true
    - updateText("https://flutter.dev") then pump 300ms: qrText stays "https://flutter.dev" (scheme already present), isUrlDetected is true
    - updateText("hello world") then pump 300ms: isUrlDetected is false (space prevents URL detection)
    - updateText("") then pump 300ms: qrText is "", isUrlDetected is false
    - Rapid typing "abc" "abcd" "abcde" within 300ms: only the final value ("abcde") sets qrText after debounce
    - copyToClipboard: ClipboardData contains the raw inputText (not qrText with https prefix)
    - dispose: no error when timer is active (cancels cleanly)
    - PermissionService.gallery methods are used by saveToGallery (not tested here — tested at integration level)
  </behavior>

  <read_first>
    - qr_scanner/lib/viewmodels/scanner_viewmodel.dart (ChangeNotifier pattern, constructor injection, private fields with getters, notifyListeners)
    - qr_scanner/lib/services/permission_service.dart (updated with gallery methods)
    - qr_scanner/test/viewmodels/scanner_viewmodel_test.dart (mock setup pattern with mocktail, group/test structure, timer pump pattern)
    - .planning/phases/04-qr-generation/04-RESEARCH.md (GeneratorViewModel code example at lines 348-421, debounce pattern, URL detection regex)
    - .planning/phases/04-qr-generation/04-CONTEXT.md (D-03 debounce approach, D-04 URL detection strategy, D-06 ChangeNotifier pattern, D-07 ViewModel state)
  </read_first>

  <action>
1. Create `qr_scanner/lib/viewmodels/generator_viewmodel.dart` following the ScannerViewModel pattern (ChangeNotifier, constructor injection, private fields with getters). Per D-06 and D-07:

   - Import `dart:async`, `flutter/clipboard.dart`, `flutter/foundation.dart`, and `../services/permission_service.dart`
   - Class `GeneratorViewModel extends ChangeNotifier` with constructor taking `PermissionService permissionService` (named parameter)
   - Private state fields with public getters: `_inputText` (String), `_qrText` (String), `_isUrlDetected` (bool), `_debounceTimer` (Timer?)
   - `updateText(String text)` method per D-03: sets `_inputText`, cancels existing `_debounceTimer`, creates new `Timer(Duration(milliseconds: 300), ...)` that calls `_qrText = _processText(text)` and `_isUrlDetected = _detectUrl(text)` then `notifyListeners()`. Also calls `notifyListeners()` immediately for the counter update.
   - `_detectUrl(String text)`: returns true if text starts with `https?://`, `ftp://`, or `www.`; OR if text contains `.` and does not contain spaces. Per D-04.
   - `_processText(String text)`: if empty return `""`; if `_detectUrl(text)` is true and text does not start with `https?://` or `ftp://`, prepend `https://`. Return processed text.
   - `copyToClipboard()`: calls `Clipboard.setData(ClipboardData(text: _inputText))` — copies raw input, not processed QR text.
   - `dispose()`: cancels `_debounceTimer` then calls `super.dispose()`.
   - Do NOT implement `saveToGallery()` or `shareImage()` yet — those are in Plan 04-02.

2. Create `qr_scanner/test/viewmodels/generator_viewmodel_test.dart` per the test map in RESEARCH.md:
   - Use `mocktail` for mocking: `class MockPermissionService extends Mock implements PermissionService {}`
   - Group: `'GeneratorViewModel Text Update and Debounce'`
     - Test: `updateText sets inputText immediately` — call `updateText("abc")`, expect `viewModel.inputText == "abc"` without pumping
     - Test: `debounce updates qrText after 300ms` — call `updateText("hello")`, pump 300ms, expect `qrText == "hello"`
     - Test: `debounce cancels previous timer on rapid input` — call `updateText("ab")` then `updateText("abcd")` within <300ms, pump 300ms, expect `qrText == "abcd"` not "ab"
     - Test: `empty text results in empty qrText` — pump 300ms after `updateText("")`, expect `qrText == ""`
   - Group: `'GeneratorViewModel URL Detection'`
     - Test: `detects URL with www prefix` — `updateText("www.example.com")`, pump 300ms, expect `isUrlDetected == true`
     - Test: `detects URL with https scheme` — `updateText("https://flutter.dev")`, pump 300ms, expect `isUrlDetected == true`
     - Test: `detects URL with common TLD` — `updateText("example.com")`, pump 300ms, expect `isUrlDetected == true`
     - Test: `does not detect URL with spaces` — `updateText("hello world.com")`, pump 300ms, expect `isUrlDetected == false`
     - Test: `prepends https:// to URL without scheme` — `updateText("example.com")`, pump 300ms, expect `qrText == "https://example.com"`
     - Test: `does not double-prepend https://` — `updateText("https://example.com")`, pump 300ms, expect `qrText == "https://example.com"`
   - Group: `'GeneratorViewModel Actions'`
     - Test: `copyToClipboard copies inputText` — use `tester.pumpWidget` with a simple widget wrapping the viewModel, call `copyToClipboard()`, verify clipboard contains `_inputText` via `Clipboard.getData`
     - Test: `dispose cancels timer` — start a debounce, immediately dispose, pump 300ms, no error thrown
  </action>

  <acceptance_criteria>
    - File `qr_scanner/lib/viewmodels/generator_viewmodel.dart` exists and imports `dart:async`, `flutter/clipboard.dart`, `flutter/foundation.dart`, `../services/permission_service.dart`
    - `GeneratorViewModel` extends `ChangeNotifier` and has constructor `GeneratorViewModel({required PermissionService permissionService})`
    - `inputText` getter returns the current input text value
    - `qrText` getter returns the QR-encoded text (with https:// prefix for URLs without scheme)
    - `isUrlDetected` getter returns true when input matches URL pattern
    - `updateText(String)` debounces 300ms before updating qrText and isUrlDetected
    - `copyToClipboard()` calls `Clipboard.setData` with the raw inputText
    - `dispose()` calls `_debounceTimer?.cancel()` before `super.dispose()`
    - Test file `qr_scanner/test/viewmodels/generator_viewmodel_test.dart` exists with 10+ test cases covering debounce, URL detection, clipboard, and dispose
    - `flutter test test/viewmodels/generator_viewmodel_test.dart` exits 0 (all tests pass)
  </acceptance_criteria>

  <verify>
    <automated>cd qr_scanner && flutter test test/viewmodels/generator_viewmodel_test.dart 2>&1 | tail -20</automated>
  </verify>

  <done>
    GeneratorViewModel exists with debounce, URL detection, clipboard copy, and proper dispose. All 10+ unit tests pass covering debounce timing, URL pattern detection, https prepend logic, clipboard copy, and timer cancellation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text input → ViewModel | Untrusted string crosses into URL detection and QR encoding |
| ViewModel → Clipboard | Text is placed into system clipboard (user-visible) |
| ViewModel → Permission handler | Permission request crosses platform boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | URL detection | medium | mitigate | URL detection validates patterns (schemes, TLDs, no spaces); does not blindly trust input |
| T-04-02 | Information Disclosure | User content logging | high | mitigate | No logging or network calls that transmit user-generated content; test verifies no external calls in ViewModel |
| T-04-03 | Denial of Service | Excessive text input | low | mitigate | TextField maxLength 250 enforced at UI layer (Plan 04-02); ViewModel accepts any string length |
| T-04-SC | Tampering | Package installs | high | mitigate | All three packages (qr_flutter, share_plus, saver_gallery) are Approved in Package Legitimacy Audit |
</threat_model>

<verification>
- `flutter pub get` succeeds with new dependencies
- `flutter test test/viewmodels/generator_viewmodel_test.dart` passes all tests
- `GeneratorViewModel` compiles and follows ChangeNotifier pattern from ScannerViewModel
- PermissionService compiles with new gallery methods
</verification>

<success_criteria>
- GeneratorViewModel exists with debounce, URL detection, clipboard, and dispose
- PermissionService extended with gallery permission methods
- Three packages installed (qr_flutter, share_plus, saver_gallery)
- All unit tests pass
</success_criteria>

<output>
Create `.planning/phases/04-qr-generation/04-01-SUMMARY.md` when done
</output>
