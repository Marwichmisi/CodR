---
phase: 01-foundation-navigation
plan: 04
type: gap_closure
source: 01-UAT.md
created: 2026-06-27T12:25:00Z
status: ready
---

# Phase 1 Gap Closure: Fix Shader Build Artifact Issue

## Goal
Resolve the stale shader build artifact that causes 1 test to fail (27/28 → 28/28 tests passing)

## Root Cause
Stale `ink_sparkle.frag` shader in build directory with incompatible runtime stage data. The NavigationBar widget (Material 3) uses InkSparkle by default, which loads the shader via a fire-and-forget Future with no error handler.

## Tasks

### Task 1: Clean build artifacts
- Run `flutter clean` to remove stale build artifacts
- Verify all 28 tests pass after clean build
- Commit: `fix(01): clean stale shader artifacts to fix test failure`

## Verification
- Run `flutter test` - all 28 tests should pass
- Run `flutter analyze` - no new issues

## Risk
Low - this is a build artifact cleanup, no code changes required
