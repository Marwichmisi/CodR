---
phase: 03-scan-results-content-display
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - qr_scanner/lib/widgets/scan_result_bottom_sheet.dart
  - qr_scanner/lib/screens/scanner_screen.dart
  - qr_scanner/test/viewmodels/result_viewmodel_test.dart
  - qr_scanner/test/screens/scan_result_bottom_sheet_test.dart
autonomous: true
requirements:
  - SCAN-08
  - QUAL-01
  - QUAL-04
must_haves:
  truths:
    - L'état d'erreur du bottom sheet affiche une icône warning rouge, un message explicite, et les boutons Réessayer et Fermer (D-13)
    - Le bouton Réessayer ferme le bottom sheet et relance le scan (D-14)
    - Les boutons Copier et Partager sont masqués en cas d'erreur (D-16)
    - Les tests unitaires du ResultViewModel couvrent la détection de type et les actions (QUAL-01)
    - Les tests widget du bottom sheet vérifient l'affichage pour chaque type de contenu (SCAN-08)
    - Les tests widget vérifient l'état d'erreur avec les bons boutons (QUAL-04)
    - La caméra se met en pause quand le bottom sheet est ouvert et reprend à la fermeture (SCAN-08)
    - Les deux modes de fermeture (swipe et bouton) reprennent la caméra de la même façon (D-10)
    - Share button est désactivé si contenu vide/erreur (backstop R6)
    - Retry répété relance le scan sans crash (backstop R7)
    - Le ViewModel gère null/empty sans exception (backstop R9)
  artifacts:
    - qr_scanner/test/viewmodels/result_viewmodel_test.dart
    - qr_scanner/test/screens/scan_result_bottom_sheet_test.dart
  key_links:
    - Les tests unitaires valident la logique de détection de type dans ResultViewModel
    - Les tests widget valident l'affichage correct du bottom sheet pour chaque état
    - Le cycle de vie caméra est vérifié via les tests du ScannerScreen
---

<objective>
Compléter la gestion d'erreur du bottom sheet avec état dédié (D-13 à D-16), écrire les tests unitaires du ResultViewModel (QUAL-01) et les tests widget du bottom sheet (QUAL-04) pour valider tous les états et interactions.

Purpose: Garantir la qualité du code via des tests automatisés et une gestion d'erreur robuste, couvrant les exigences QUAL-01, QUAL-04 et les backstops du SPEC Edge Coverage.
Output: Tests unitaires ResultViewModel complets, tests widget bottom sheet, gestion d'erreur finalisée.
</objective>

<execution_context>
@/home/marwane/Documents/CodR/.opencode/gsd-core/workflows/execute-plan.md
@/home/marwane/Documents/CodR/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scan-results-content-display/03-CONTEXT.md
@.planning/phases/03-scan-results-content-display/03-RESEARCH.md
@.planning/phases/03-scan-results-content-display/03-SPEC.md
@.planning/phases/03-scan-results-content-display/03-PATTERNS.md
@.planning/phases/03-scan-results-content-display/03-UI-SPEC.md
@.planning/phases/03-scan-results-content-display/03-VALIDATION.md
@.planning/phases/03-scan-results-content-display/03-01-SUMMARY.md
@qr_scanner/lib/widgets/scan_result_bottom_sheet.dart
@qr_scanner/lib/viewmodels/result_viewmodel.dart
@qr_scanner/lib/screens/scanner_screen.dart
@qr_scanner/test/viewmodels/result_viewmodel_test.dart
@qr_scanner/test/screens/scanner_screen_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests unitaires ResultViewModel — détection, actions et backstops</name>
  <files>qr_scanner/test/viewmodels/result_viewmodel_test.dart</files>
  <read_first>
    - qr_scanner/lib/viewmodels/result_viewmodel.dart (ViewModel à tester)
    - qr_scanner/lib/models/content_type.dart (enum à tester)
    - qr_scanner/test/viewmodels/scanner_viewmodel_test.dart (pattern de test à suivre)
    - .planning/phases/03-scan-results-content-display/03-PATTERNS.md (pattern pour result_viewmodel_test.dart)
    - .planning/phases/03-scan-results-content-display/03-VALIDATION.md (test map et sampling rate)
    - .agents/skills/flutter-add-widget-test/SKILL.md (workflow de test)
  </read_first>
  <behavior>
    - Groupe "Content Detection" : test détection URL (http et https), email, phone, texte, empty
    - Test D-08 : priorité URL > email > téléphone (un seul type retourné)
    - Test D-08 : contenu mixte "Mon site: https://flutter.dev" → text
    - Test D-15 : contenu whitespace-only → empty + hasError=true
    - Test D-05 : schéma javascript:/file:/data: → text (pas URL)
    - Test D-05 : http://example.com → url (http accepté)
    - Groupe "Actions" : test copyToClipboard, shareContent, openUrl, sendEmail, callPhone
    - Backstop R6 : shareContent avec contenu vide → pas de crash
    - Backstop R7 : detectContentType appelé plusieurs fois successivement → pas de crash
    - Backstop R9 : detectContentType avec null ou chaîne vide → pas d'exception
  </behavior>
  <action>
    Créer ou compléter `qr_scanner/test/viewmodels/result_viewmodel_test.dart` :

    Structure des tests (pattern de scanner_viewmodel_test.dart) :
    - Imports : flutter_test, mocktail, content_type, result_viewmodel
    - setUp : initialiser ResultViewModel
    - group('Content Detection') :
      - test : détecte URL https → ContentType.url
      - test : détecte URL http → ContentType.url (D-05)
      - test : détecte email → ContentType.email (D-06)
      - test : détecte téléphone international → ContentType.phone (D-07)
      - test : détecte téléphone national → ContentType.phone
      - test : détecte texte brut → ContentType.text
      - test : contenu vide → ContentType.empty + hasError=true
      - test : contenu whitespace-only → ContentType.empty + hasError=true (D-15)
      - test : priorité URL > email (contenant les deux) → ContentType.url (D-08)
      - test : contenu mixte "Mon site: https://flutter.dev" → ContentType.text (D-08)
      - test : schéma javascript: → ContentType.text (D-05)
      - test : schéma file: → ContentType.text (D-05)
    - group('Actions') :
      - test : copyToClipboard copie le contenu (mock Clipboard)
      - test : shareContent appelle SharePlus.instance.share
      - test : openUrl appelle launchUrl avec le bon Uri
      - test : sendEmail crée Uri mailto: correct
      - test : callPhone crée Uri tel: correct
    - group('Edge Cases (Backstops)') :
      - test R6 : shareContent avec contenu vide ne crash pas
      - test R7 : detectContentType appelé 10 fois de suite ne crash pas
      - test R9 : detectContentType avec chaîne vide ne lève pas d'exception

    Pour les tests d'actions, utiliser mocktail pour mocker les packages externes (url_launcher, share_plus, Clipboard). Si les méthodes sont trop simples pour mocker, tester que le résultat est cohérent.
  </action>
  <verify>
    <automated>cd qr_scanner && flutter test test/viewmodels/result_viewmodel_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier result_viewmodel_test.dart existe
    - Au moins 12 tests couvrent la détection de type (URL http/https, email, phone international/national, text, empty, whitespace, priorité, mixte, schémas rejetés)
    - Au moins 5 tests couvrent les actions (copy, share, openUrl, sendEmail, callPhone)
    - Au moins 3 tests backstops (R6, R7, R9) sont présents
    - Tous les tests passent (flutter test test/viewmodels/result_viewmodel_test.dart)
    - Les tests vérifient les comportements spécifiés dans le SPEC (D-05 à D-08, D-15)
  </acceptance_criteria>
  <done>
    - Tests unitaires ResultViewModel complets avec couverture de détection, actions et backstops
    - Tous les tests passent
    - Exigence QUAL-01 satisfaite
  </done>
</task>

<task type="auto">
  <name>Task 2: Tests widget bottom sheet — affichage, actions et état d'erreur</name>
  <files>qr_scanner/test/screens/scan_result_bottom_sheet_test.dart</files>
  <read_first>
    - qr_scanner/lib/widgets/scan_result_bottom_sheet.dart (widget à tester)
    - qr_scanner/lib/models/content_type.dart (enum pour configurer les tests)
    - qr_scanner/lib/viewmodels/result_viewmodel.dart (ViewModel pour initialiser les tests)
    - qr_scanner/test/screens/scanner_screen_test.dart (pattern de widget test à suivre)
    - .planning/phases/03-scan-results-content-display/03-PATTERNS.md (pattern pour bottom sheet test)
    - .planning/phases/03-scan-results-content-display/03-UI-SPEC.md (copywriting contract pour les textes des boutons)
    - .agents/skills/flutter-add-widget-test/SKILL.md (workflow de test widget)
  </read_first>
  <action>
    Créer `qr_scanner/test/screens/scan_result_bottom_sheet_test.dart` :

    Structure (pattern de scanner_screen_test.dart) :
    - Imports : flutter/material.dart, flutter_test, mocktail, content_type, result_viewmodel, scan_result_bottom_sheet, app_theme
    - Fonction helper `buildApp({String content})` : crée MaterialApp avec theme, Scaffold, et ScanResultBottomSheet
    - setUp : initialiser ResultViewModel

    Tests à écrire :
    - group('Content Display') :
      - testWidgets : affiche le contenu scanné pour du texte brut
      - testWidgets : affiche "Ouvrir le lien" pour une URL (D-01, copywriting: "Ouvrir le lien")
      - testWidgets : affiche "Envoyer l'e-mail" pour un email (copywriting: "Envoyer l'e-mail")
      - testWidgets : affiche "Appeler le numéro" pour un téléphone (copywriting: "Appeler le numéro")
      - testWidgets : affiche Copier et Partager pour tout type de contenu
      - testWidgets : n'affiche PAS de bouton contextuel pour du texte brut

    - group('Error State') :
      - testWidgets : état d'erreur affiche icône warning, message, boutons Réessayer et Fermer (D-13)
      - testWidgets : boutons Copier et Partager masqués en état d'erreur (D-16)
      - testWidgets : contenu whitespace-only affiche l'état d'erreur (D-15)

    - group('Responsive Layout') :
      - testWidgets : utilise LayoutBuilder (vérifier que le widget est bien dans l'arbre)

    - group('Actions') :
      - testWidgets : tap sur Copier appelle copyToClipboard du viewModel
      - testWidgets : tap sur Partager appelle shareContent du viewModel

    Utiliser les textes exacts du Copywriting Contract (UI-SPEC.md) :
    - "Ouvrir le lien" pour URL
    - "Envoyer l'e-mail" pour email
    - "Appeler le numéro" pour phone
    - "Copier le texte" pour copy
    - "Partager le contenu" pour share
    - "Code QR vide ou invalide" pour erreur
    - "Réessayer" et "Fermer" pour les boutons d'erreur

    Utiliser mocktail pour mocker les méthodes du ResultViewModel si nécessaire (openUrl, sendEmail, etc.)
  </action>
  <verify>
    <automated>cd qr_scanner && flutter test test/screens/scan_result_bottom_sheet_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier scan_result_bottom_sheet_test.dart existe
    - Au moins 8 tests widget couvrent l'affichage pour chaque type de contenu
    - Au moins 3 tests widget couvrent l'état d'erreur (D-13, D-15, D-16)
    - Au moins 2 tests widget vérifient les actions (copy, share)
    - Les textes des boutons correspondent au Copywriting Contract (UI-SPEC.md)
    - Tous les tests passent (flutter test test/screens/scan_result_bottom_sheet_test.dart)
    - Les tests vérifient les exigences SCAN-08 et QUAL-04
  </acceptance_criteria>
  <done>
    - Tests widget bottom sheet complets avec couverture des états de contenu et d'erreur
    - Tous les tests passent
    - Exigences SCAN-08 et QUAL-04 satisfaites via tests automatisés
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalisation gestion d'erreur et validation complète du cycle de vie caméra</name>
  <files>qr_scanner/lib/widgets/scan_result_bottom_sheet.dart, qr_scanner/lib/screens/scanner_screen.dart</files>
  <read_first>
    - qr_scanner/lib/widgets/scan_result_bottom_sheet.dart (widget existant)
    - qr_scanner/lib/screens/scanner_screen.dart (intégration existante)
    - qr_scanner/lib/viewmodels/result_viewmodel.dart (ViewModel)
    - .planning/phases/03-scan-results-content-display/03-SPEC.md (Acceptance Criteria complets)
    - .planning/phases/03-scan-results-content-display/03-CONTEXT.md (D-13 à D-16)
    - .planning/phases/03-scan-results-content-display/03-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <action>
    Vérifier et finaliser l'intégration complète :

    1. Vérifier scan_result_bottom_sheet.dart — l'état d'erreur est correctement implémenté :
       - D-13 : icône warning rouge, message "Code QR vide ou invalide", boutons "Réessayer" et "Fermer"
       - D-16 : boutons Copier et Partager masqués en état d'erreur
       - Le callback onRetry est bien passé au widget et déclenche Navigator.pop

    2. Vérifier scanner_screen.dart — le cycle de vie caméra est correct :
       - D-09 : _controller.stop() appelé à l'ouverture
       - D-10 : _controller.start() dans whenComplete, quel que soit le mode de fermeture
       - D-11 : vérification de _controller.isInitialized avant start/stop
       - D-12 : debounce de 1s après reprise

    3. Exécuter la suite complète de tests :
       - `flutter test test/viewmodels/result_viewmodel_test.dart`
       - `flutter test test/screens/scan_result_bottom_sheet_test.dart`
       - `flutter test` (suite complète)

    4. Vérifier que les backstops sont bien testés :
       - R6 : Share button désactivé si contenu vide/erreur
       - R7 : Retry répété relance le scan sans crash
       - R9 : ViewModel gère null/empty sans exception

    5. Vérifier les MUST NOT du SPEC :
       - Aucune URL n'est ouverte automatiquement sans interaction utilisateur
       - Les appels téléphoniques passent par le dialer (tel:), jamais d'appel direct
       - Les schémas non-http(s) ne sont pas ouverts dans le navigateur
  </action>
  <verify>
    <automated>cd qr_scanner && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - État d'erreur du bottom sheet implémenté et testé (D-13, D-14, D-15, D-16)
    - Cycle de vie caméra complet : pause → bottom sheet → reprise (D-09 à D-12)
    - La suite complète de tests passe (flutter test)
    - Les backstops R6, R7, R9 sont validés par des tests
    - Les MUST NOT sont respectés (pas d'ouverture automatique, tel: uniquement, schémas filtrés)
    - flutter analyze passe sans erreur
    - L'app compile et fonctionne (flutter build apk --debug)
  </acceptance_criteria>
  <done>
    - Gestion d'erreur du bottom sheet finalisée et testée
    - Cycle de vie caméra complet validé
    - Tous les tests passent (unitaires + widget)
    - Exigences SCAN-08, QUAL-01, QUAL-04 toutes satisfaites
    - Tous les backstops du SPEC Edge Coverage validés
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Production code | Les tests valident que le code de production se comporte correctement |
| Error state → User interaction | L'état d'erreur guide l'utilisateur vers une action corrective |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Elevation | Incomplete error handling | medium | mitigate | Tests widget valident tous les états d'erreur avec les bons boutons |
| T-03-06 | Tampering | Camera not resuming after sheet close | high | mitigate | Tests de cycle de vie caméra + whenComplete callback vérifié |
| T-03-07 | Information Disclosure | Share with empty content | low | mitigate | Backstop R6 : test unitaire vérifie pas de crash avec contenu vide |
</threat_model>

<verification>
1. `cd qr_scanner && flutter test` — tous les tests passent (unitaires + widget)
2. `cd qr_scanner && flutter analyze` — aucune erreur
3. Vérification manuelle : scanner un QR vide affiche l'état d'erreur avec Réessayer/Fermer
4. Vérification manuelle : taper Réessayer ferme le bottom sheet et la caméra reprend
5. Vérification manulaire : scanner un QR URL puis fermer par swipe reprend la caméra
</verification>

<success_criteria>
- Tests unitaires ResultViewModel : ≥12 tests couvrant détection, actions, backstops
- Tests widget bottom sheet : ≥13 tests couvrant affichage, erreur, actions
- Tous les tests passent (flutter test)
- Gestion d'erreur conforme au SPEC (D-13 à D-16)
- Cycle de vie caméra complet et testé
- Backstops R6, R7, R9 validés
</success_criteria>

<output>
Create `.planning/phases/03-scan-results-content-display/03-02-SUMMARY.md` when done
</output>
